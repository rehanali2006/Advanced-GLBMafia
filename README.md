To run this project-
1) You have to first the project.
2) Install dependencies using npm install
3) start redis server in wsl by typing redis-server
4) now start the server node app.js
5) got to localhost:8000
